// Routing page JS
import { API_BASE_URL, getAuthHeader } from './utils.js';

document.getElementById('routing-form').addEventListener('submit', async (e) => {
    e.preventDefault();
    // Fetch safe route from API and display on map
});